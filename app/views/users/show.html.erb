<p id="notice"><%= notice %></p>

<div class="well">
<table class="table">
	<tr>
		<th>Name</th>
		<th>Email</th>
		<th>ID</th>
	</tr>
	
	<tr>
		<th><%= best_in_place @user, :name, :activator => "#editname" %>
			<a id="editname" href="#"><i class="icon-pencil"></i></a>
		</th>
		<th><%= best_in_place @user, :email, :activator => "#editmail" %>
			<a id="editmail" href="#"><i class="icon-pencil"></i></a> </th>
		<th><%= @user.id %></th>
	</tr>
	
</table>
</div>

