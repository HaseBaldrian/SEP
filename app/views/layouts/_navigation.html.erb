<div id="user_nav">

  	<ul class="nav">
  		 <li>
		    <%= link_to "Veranstaltungsübersicht", user_events_path(session[:user_id]) %> 
		 </li>
		 <li class="divider-vertical"></li>
		 <li> 	
		    <%= link_to "Profil bearbeiten", edit_user_path(session[:user_id]) %>
		 </li>
		 <% if session[:user_id] == 1 %>
		 <li class="divider-vertical"></li>
		 <li>		    
		    <%= link_to "Userübersicht", users_path %>		  
		 </li>
		 <% end %>
		 <li class="divider-vertical"></li>
		 <li>
		 	<%= link_to "Logout", logout_path, :method => :delete%>
		 </li>
	</ul>
	
	<!-- Test Dropdownmenue-->
	<!--
	<div class="dropdown">
		<a class="dropdown-toggle" href="#" data-toggle="dropdown" >
			TEXT <b class="caret"></b></a>
		<ul class="dropdown-menu" role="menu" aria-labelledby="dropdownMenu">
			<li><%= link_to "Logout", logout_path, :method => :delete, :class => "btn"%></li>
			<li><%= link_to "Edit Profile", edit_user_path(session[:user_id]), :class => "btn" %></li>
			<li><%= link_to "Eventlist", user_events_path(session[:user_id]), :class => "btn" %> </li>
			<% if session[:user_id] == 19 %>
		    	<li><%= link_to "Administrate Users", users_path, :class => "btn" %></li>
		    <% end %>
		</ul>
	</div>
	-->
	<!-- Test Dropdownmenue ende -->
	
</div>